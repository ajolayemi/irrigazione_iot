{
  "scripts": {
    "update-local-types": "npx supabase gen types typescript --local > functions/schemas/database.types.ts",
    "update-types": "npx supabase gen types typescript --project-id \"$PROJECT_REF\" > functions/schemas/database.types.ts"
  }
}
